apiVersion: v1
kind: Service
metadata:
  name: clientesapp-api-service
  namespace: clientesapp
spec:
  type: LoadBalancer
  selector:
    app: clientesapp-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080